export enum Bearing {
    NORTH,
    SOUTH,
    EAST,
    WEST
}

export enum Direction {
    LEFT,
    RIGHT
}

export enum ValidCommands {
    PLACE,
    MOVE,
    LEFT,
    RIGHT,
    REPORT,
    DESCRIPTION,
    QUIT
}